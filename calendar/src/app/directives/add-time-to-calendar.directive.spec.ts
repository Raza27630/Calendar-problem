import { AddTimeToCalendarDirective } from './add-time-to-calendar.directive';

describe('AddTimeToCalendarDirective', () => {
  it('should create an instance', () => {
    const directive = new AddTimeToCalendarDirective();
    expect(directive).toBeTruthy();
  });
});
